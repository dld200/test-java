spring:
  datasource:
    url: jdbc:mysql://localhost:3306/app?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: xxxxxxxx
  jpa:
    hibernate:
      ddl-auto: update  # 可选: create, update, validate
    show-sql: true      # 可选: 打印 SQL
    database-platform: org.hibernate.dialect.MySQL8Dialect
#  ai:
#    mcp:
#      server:
#        name: webmvc-mcp-server
#        version: 1.0.0
#        type: SYNC
#        instructions: "This server provides weather information tools and resources"
#        sse-message-endpoint: /mcp/messages
#        capabilities:
#          tool: true
#          resource: true
#          prompt: true
#          completion: true

logging:
  level:
    org.example.server.dao: debug
    org.mybatis: debug