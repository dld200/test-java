spring:
  datasource:
    main:
      jdbc-url: jdbc:mysql://localhost:3306/app?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC
      driver-class-name: com.mysql.cj.jdbc.Driver
      username: root
      password: xxxxxxxx
    stage:
      jdbc-url: jdbc:mysql://10.0.70.52:3306
      username: lawli
      password: HYlsdjsdfyt7@p
      driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update  # 可选: create, update, validate
    show-sql: true      # 可选: 打印 SQL
    database-platform: org.hibernate.dialect.MySQL8Dialect
logging:
  level:
    org.example.server.dao: debug
    org.mybatis: debug

server:
  upload-dir: /Users/snap/workspace/app-agent/uploads
